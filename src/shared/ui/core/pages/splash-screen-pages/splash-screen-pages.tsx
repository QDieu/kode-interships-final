import React from 'react';
import { Logo } from '../../molecules';
import { SplashScreenTemplate } from '../../templates';

export const SplashScreenPages = ({}) => {
  return <SplashScreenTemplate logo={<Logo bigSize="big" />} />;
};
